# Floaty

桌面悬浮便签应用。每个便签是一个独立的无边框置顶窗口，通过系统托盘管理。

## 技术栈

Tauri 2 + React 19 + TipTap 3 + SQLite + Tailwind CSS 4

## 开发

```bash
npm install
npm run tauri dev
```

## 构建

```bash
npm run tauri build
```
